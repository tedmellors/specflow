#+TITLE: org-store – TODO
#+STARTUP: overview
#+SEQ_TODO: TODO NEXT WAITING | DONE

* About

Outcome-based tasks for org-store implementation.
Derived from =units/org-store/spec.org=.

* Implement

** DONE Discovery: specflow-org-store-find-control-plane
   CLOSED: [2025-01-19]
   Given a buffer in a SpecFlow project,
   when =specflow-org-store-find-control-plane= is called,
   then it returns the absolute path to =docs/specflow.org=.

   Outcomes:
   - Searches upward from =default-directory=
   - Stops at project root (=.git=, =.projectile=, =.specflow-root=)
   - Falls back to =specflow-control-plane-path= variable
   - Signals =specflow-control-plane-not-found= if not found

** DONE Read: specflow-org-store-read-project-state
   CLOSED: [2025-01-19]
   Given a valid control plane file,
   when =specflow-org-store-read-project-state= is called,
   then it returns a plist with =:phase=, =:active-unit=, =:control-plane-path=.

   Outcomes:
   - Parses =SPEC_FLOW_PHASE= and =SPEC_FLOW_ACTIVE_UNIT= properties
   - Signals =specflow-control-plane-malformed= if required properties missing

** DONE Read: specflow-org-store-read-unit
   CLOSED: [2025-01-19]
   Given a unit name and control plane,
   when =specflow-org-store-read-unit= is called,
   then it returns the unit entry plist.

   Outcomes:
   - Returns plist with =:name=, =:spec=, =:todo=, =:rules=, =:parent=, etc.
   - Signals =specflow-unit-not-found= if unit not in registry
   - Signals =specflow-unit-malformed= if required properties missing
   - Returns =nil= for missing optional properties

** DONE Read: specflow-org-store-read-unit-registry
   CLOSED: [2025-01-19]
   Given a control plane file,
   when =specflow-org-store-read-unit-registry= is called,
   then it returns a list of all unit entry plists in document order.

** DONE Write: specflow-org-store-write-property
   CLOSED: [2025-01-19]
   Given a file path, heading path, property name, and value,
   when =specflow-org-store-write-property= is called,
   then the property is written with minimal diff.

   Outcomes:
   - Existing property: value replaced in-place
   - Missing property: inserted at end of drawer
   - Missing drawer: drawer created with property
   - No other file content modified (byte-for-byte)
   - Signals =specflow-heading-not-found= if heading invalid
   - Signals =specflow-file-not-writable= if cannot save

** DONE Validate: specflow-org-store-validate-unit-pointers
   CLOSED: [2025-01-19]
   Given a unit entry plist,
   when =specflow-org-store-validate-unit-pointers= is called,
   then it returns =t= if SPEC/TODO/RULES files exist, else signals error.

** DONE Validate: specflow-org-store-validate-parent-chain
   CLOSED: [2025-01-19]
   Given a unit name,
   when =specflow-org-store-validate-parent-chain= is called,
   then it returns list of ancestor names, or signals error if parent missing.

* Test

** DONE ERT: Discovery tests
   CLOSED: [2025-01-19]
   - File found at project root
   - File found 2 levels up
   - Fallback to =specflow-control-plane-path=
   - Error when not found
   11 tests, all passing.

** DONE ERT: Project state tests
   CLOSED: [2025-01-19]
   - Valid parse returns correct plist
   - Missing =SPEC_FLOW_PHASE= signals error
   - Missing =SPEC_FLOW_ACTIVE_UNIT= signals error
   - No Project heading signals error
   - Auto-discovery of control plane
   - Determinism test
   6 tests, all passing.

** DONE ERT: Unit read tests
   CLOSED: [2025-01-19]
   - Valid unit returns correct plist
   - Missing unit signals =specflow-unit-not-found=
   - Missing required property signals =specflow-unit-malformed=
   - Missing optional property returns =nil=
   - Unit with CHILDREN property
   - Determinism test
   8 tests, all passing.

** DONE ERT: Unit registry tests
   CLOSED: [2025-01-19]
   - Multiple units returned in document order
   - Empty registry returns empty list
   - Validates each unit's required properties
   - Determinism test
   - Each unit has complete properties
   5 tests, all passing.

** DONE ERT: Write property tests
   CLOSED: [2025-01-19]
   - Replace existing property value
   - Add new property to existing drawer
   - Create drawer when missing
   - Heading not found signals error
   - Minimal-diff: only target line changes
   - Idempotent: write same value twice → no change
   - Write then read → value matches
   - Nested heading support
   9 tests, all passing.

** DONE ERT: Unit pointer validation tests
   CLOSED: [2025-01-19]
   - Valid pointers return =t=
   - Missing SPEC signals =specflow-unit-pointer-invalid=
   - Missing TODO signals =specflow-unit-pointer-invalid=
   - Missing RULES signals =specflow-unit-pointer-invalid=
   - Multiple missing files listed in error
   - Nil path treated as missing
   - Auto-discovers project root
   7 tests, all passing.

** DONE ERT: Parent chain validation tests
   CLOSED: [2025-01-19]
   - No parent returns empty list
   - Single parent returns single-element list
   - Multi-level hierarchy returns correct ancestor list
   - Missing parent signals =specflow-parent-not-found=
   - Nonexistent unit signals =specflow-unit-not-found=
   - Circular reference detected and signaled
   - Determinism test
   7 tests, all passing.

** TODO ERT: Determinism tests
   - Read same file twice → identical output
   - Write same value twice → no file change on second write
   - Write then read → value matches

* Validate

** DONE Run full ERT suite
   CLOSED: [2025-01-19]
   53 tests, all passing.

** DONE Manual smoke test
   CLOSED: [2025-01-19]
   - Open a SpecFlow project
   - Call each public function from =M-:=
   - Verify expected output
   All functions tested and returning expected results.

* Document

** DONE Write units/org-store/README.md
   CLOSED: [2025-01-19]
   Usage examples for each public function.
