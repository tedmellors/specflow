#+TITLE: docs – Unit TODO
#+STARTUP: overview
#+SEQ_TODO: TODO NEXT WAITING | DONE

* About

This file tracks outcome-based tasks for the docs unit.
See =spec.org= for requirements.

* Active

** DONE Migrate core unit into docs
   CLOSED: [2026-01-22]
   Migrated core unit into docs: moved architecture.org and overview.org,
   updated control plane (docs is now parent), updated specflow-compose.el
   and all 4 test files. All 254 tests passing.

** NEXT Update README.md for .specflow/ structure
   - Given the new .specflow/ directory structure
   - When a user reads the root README
   - Then:
     - [ ] Quick Start shows .specflow/specflow.org (not docs/specflow.org)
     - [ ] Quick Start shows .specflow/todo.org (not root todo.org)
     - [ ] Project Structure section shows .specflow/ layout
     - [ ] No RULES property in unit examples
     - [ ] Test count updated to current (254)

* Backlog

(empty)

* Done

** DONE Write root README.md
   CLOSED: [2025-01-20]
   Given spec.org defines the required sections,
   when README.md is written,
   then it contains all 9 sections within 300 lines.

** DONE Validate README against spec
   CLOSED: [2025-01-20]
   Given README.md is written,
   when reviewed against spec.org,
   then all required sections are present and constraints are met.

** DONE Test quick start instructions
   CLOSED: [2025-01-20]
   Given README quick start section,
   when a new user follows the steps,
   then they can run specflow-hydrate-copy-header successfully.
