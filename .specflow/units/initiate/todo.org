#+TITLE: initiate – Unit TODO
#+STARTUP: overview
#+SEQ_TODO: TODO NEXT WAITING | DONE

* About

This file tracks outcome-based tasks for the initiate unit.
See =spec.org= for requirements, =CLAUDE.md= for constraints.

* Active

** DONE Update specflow-initiate for .specflow/ structure
   CLOSED: [2026-01-22]
   Given the new spec defines .specflow/ directory structure,
   when specflow-initiate is run in an empty directory,
   then the following files are created:

   Outcomes:
   - [X] Update precondition: check for .specflow/specflow.org (not docs/specflow.org)
   - [X] Create .specflow/ directory
   - [X] Write .specflow/specflow.org from template (control plane with docs unit)
   - [X] Write .specflow/todo.org from template (root tasks)
   - [X] Write .specflow/rules.org from default template
   - [X] Create .specflow/units/docs/ directory
   - [X] Write .specflow/units/docs/spec.org from template
   - [X] Write .specflow/units/docs/todo.org from template
   - [X] Create src/<project>/ directory (base for future unit source)
   - [X] Create tests/ directory
   - [X] Generate CLAUDE.md via specflow-hydrate-rules (or graceful fallback)
   - [X] Display success message with next steps

** DONE Update templates for docs unit
   CLOSED: [2026-01-22]
   Given the docs unit replaces core as the starting unit,
   when templates are generated,
   then they reflect the documentation-first approach:

   Outcomes:
   - [X] Control plane template: docs unit with SPEC and TODO (no DIR)
   - [X] Root todo template: NEXT task for "docs: Plan phase"
   - [X] Docs unit spec template: overview.org and architecture.org as deliverables
   - [X] Docs unit todo template: Draft overview.org as NEXT task

** DONE Update ERT tests for new structure
   CLOSED: [2026-01-22]
   Given specflow-initiate creates .specflow/ structure,
   when ERT tests are run,
   then all tests verify new file paths:

   Outcomes:
   - [X] Test creates .specflow/specflow.org
   - [X] Test creates .specflow/todo.org
   - [X] Test creates .specflow/rules.org
   - [X] Test creates .specflow/units/docs/spec.org
   - [X] Test creates .specflow/units/docs/todo.org
   - [X] Test creates src/<project>/ directory
   - [X] Test creates tests/ directory
   - [X] Test gracefully handles CLAUDE.md generation
   - [X] Test precondition blocks on existing .specflow/specflow.org
   - [X] All 32 tests pass

* Backlog

** TODO Delete temp-planning-artifacts.org
   Given README and spec are complete,
   when temp file is no longer needed,
   then delete units/initiate/temp-planning-artifacts.org.

* Done

** DONE Code extraction: Modular file structure
   CLOSED: [2026-01-20]
   Extracted initiate code to modular structure.
   - =units/initiate/src/specflow-initiate.el= created
   - =units/initiate/tests/test-specflow-initiate.el= with load-path setup
   - 21 ERT tests passing
   - Documentation updated

** DONE Implement specflow-initiate command
   CLOSED: [2026-01-20]
   Given spec defines file creation behavior,
   when specflow-initiate is implemented,
   then all files created per spec.

** DONE Implement precondition checks
   CLOSED: [2026-01-20]
   Given user runs specflow-initiate,
   when docs/specflow.org or .specflow exists,
   then error message displayed, no files created.

** DONE Implement control plane template
   CLOSED: [2026-01-20]
   Control plane includes project name, Plan phase, core unit.

** DONE Implement CLAUDE.md template
   CLOSED: [2026-01-20]
   CLAUDE.md includes SpecFlow rules and phase enforcement.

** DONE Implement todo.org template
   CLOSED: [2026-01-20]
   todo.org includes example NEXT task for core Plan phase.

** DONE Implement project name derivation
   CLOSED: [2026-01-20]
   Project name derived from directory, lowercase, spaces to hyphens.

** DONE Write ERT tests for initiate
   CLOSED: [2026-01-20]
   21 ERT tests covering all functionality.

** DONE Validate initiate works end-to-end
   CLOSED: [2026-01-20]
   All files created, org-store can read control plane.

** DONE Validate precondition enforcement
   CLOSED: [2026-01-20]
   Second initiate blocked with error.

** DONE Write README.md
   CLOSED: [2026-01-20]
   Documentation for specflow-initiate command.
