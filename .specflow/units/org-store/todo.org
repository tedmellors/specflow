#+TITLE: org-store – TODO
#+STARTUP: overview
#+SEQ_TODO: TODO NEXT WAITING | DONE

* About

Outcome-based tasks for org-store implementation.
Derived from =units/org-store/spec.org=.

* Implement

** DONE Discovery: specflow-org-store-find-control-plane
   CLOSED: [2025-01-19]
   Given a buffer in a SpecFlow project,
   when =specflow-org-store-find-control-plane= is called,
   then it returns the absolute path to =docs/specflow.org=.

   Outcomes:
   - Searches upward from =default-directory=
   - Stops at project root (=.git=, =.projectile=, =.specflow-root=)
   - Falls back to =specflow-control-plane-path= variable
   - Signals =specflow-control-plane-not-found= if not found

** DONE Read: specflow-org-store-read-project-state
   CLOSED: [2025-01-19]
   Given a valid control plane file,
   when =specflow-org-store-read-project-state= is called,
   then it returns a plist with =:phase=, =:active-unit=, =:control-plane-path=.

   Outcomes:
   - Parses =SPEC_FLOW_PHASE= and =SPEC_FLOW_ACTIVE_UNIT= properties
   - Signals =specflow-control-plane-malformed= if required properties missing

** DONE Read: specflow-org-store-read-unit
   CLOSED: [2025-01-19]
   Given a unit name and control plane,
   when =specflow-org-store-read-unit= is called,
   then it returns the unit entry plist.

   Outcomes:
   - Returns plist with =:name=, =:spec=, =:todo=, =:rules=, =:parent=, etc.
   - Signals =specflow-unit-not-found= if unit not in registry
   - Signals =specflow-unit-malformed= if required properties missing
   - Returns =nil= for missing optional properties

** DONE Read: specflow-org-store-read-unit-registry
   CLOSED: [2025-01-19]
   Given a control plane file,
   when =specflow-org-store-read-unit-registry= is called,
   then it returns a list of all unit entry plists in document order.

** DONE Write: specflow-org-store-write-property
   CLOSED: [2025-01-19]
   Given a file path, heading path, property name, and value,
   when =specflow-org-store-write-property= is called,
   then the property is written with minimal diff.

   Outcomes:
   - Existing property: value replaced in-place
   - Missing property: inserted at end of drawer
   - Missing drawer: drawer created with property
   - No other file content modified (byte-for-byte)
   - Signals =specflow-heading-not-found= if heading invalid
   - Signals =specflow-file-not-writable= if cannot save

** DONE Validate: specflow-org-store-validate-unit-pointers
   CLOSED: [2025-01-19]
   Given a unit entry plist,
   when =specflow-org-store-validate-unit-pointers= is called,
   then it returns =t= if SPEC/TODO/RULES files exist, else signals error.

** DONE Validate: specflow-org-store-validate-parent-chain
   CLOSED: [2025-01-19]
   Given a unit name,
   when =specflow-org-store-validate-parent-chain= is called,
   then it returns list of ancestor names, or signals error if parent missing.

* Test

** DONE ERT: Discovery tests
   CLOSED: [2025-01-19]
   - File found at project root
   - File found 2 levels up
   - Fallback to =specflow-control-plane-path=
   - Error when not found
   11 tests, all passing.

** DONE ERT: Project state tests
   CLOSED: [2025-01-19]
   - Valid parse returns correct plist
   - Missing =SPEC_FLOW_PHASE= signals error
   - Missing =SPEC_FLOW_ACTIVE_UNIT= signals error
   - No Project heading signals error
   - Auto-discovery of control plane
   - Determinism test
   6 tests, all passing.

** DONE ERT: Unit read tests
   CLOSED: [2025-01-19]
   - Valid unit returns correct plist
   - Missing unit signals =specflow-unit-not-found=
   - Missing required property signals =specflow-unit-malformed=
   - Missing optional property returns =nil=
   - Unit with CHILDREN property
   - Determinism test
   8 tests, all passing.

** DONE ERT: Unit registry tests
   CLOSED: [2025-01-19]
   - Multiple units returned in document order
   - Empty registry returns empty list
   - Validates each unit's required properties
   - Determinism test
   - Each unit has complete properties
   5 tests, all passing.

** DONE ERT: Write property tests
   CLOSED: [2025-01-19]
   - Replace existing property value
   - Add new property to existing drawer
   - Create drawer when missing
   - Heading not found signals error
   - Minimal-diff: only target line changes
   - Idempotent: write same value twice → no change
   - Write then read → value matches
   - Nested heading support
   9 tests, all passing.

** DONE ERT: Unit pointer validation tests
   CLOSED: [2025-01-19]
   - Valid pointers return =t=
   - Missing SPEC signals =specflow-unit-pointer-invalid=
   - Missing TODO signals =specflow-unit-pointer-invalid=
   - Missing RULES signals =specflow-unit-pointer-invalid=
   - Multiple missing files listed in error
   - Nil path treated as missing
   - Auto-discovers project root
   7 tests, all passing.

** DONE ERT: Parent chain validation tests
   CLOSED: [2025-01-19]
   - No parent returns empty list
   - Single parent returns single-element list
   - Multi-level hierarchy returns correct ancestor list
   - Missing parent signals =specflow-parent-not-found=
   - Nonexistent unit signals =specflow-unit-not-found=
   - Circular reference detected and signaled
   - Determinism test
   7 tests, all passing.

** DONE ERT: Determinism tests
   CLOSED: [2026-01-20]
   Covered by existing tests:
   - specflow-test-find-control-plane-deterministic
   - specflow-test-read-project-state-deterministic
   - specflow-test-read-unit-deterministic
   - specflow-test-read-unit-registry-deterministic
   - specflow-test-write-property-idempotent
   - specflow-test-validate-parent-chain-deterministic
   - specflow-test-write-then-read-property

* Workflow Commands

** DONE Implement: specflow-phase-shift
   CLOSED: [2026-01-20]
   Given a valid phase name,
   when =specflow-phase-shift= is called interactively,
   then it updates SPEC_FLOW_PHASE in control plane and displays confirmation.

   Outcomes:
   - Prompts with =completing-read= from valid phase list
   - Valid phases: Plan, Specify, Scaffold, Implement, Validate, Document
   - Writes to =("Project")= heading in control plane
   - Signals =user-error= if phase is invalid
   - Displays "Phase changed to <PHASE>" on success

** DONE Implement: specflow-add-root-task
   CLOSED: [2026-01-20]
   Given a task headline,
   when =specflow-add-root-task= is called interactively,
   then it appends a TODO entry to root todo.org under Backlog section.

   Outcomes:
   - Prompts for headline string
   - Finds project root from control plane path
   - Opens =todo.org= at project root
   - Finds =Backlog= heading (level 1)
   - Inserts =** TODO <HEADLINE>= at end of Backlog section
   - Saves file with minimal diff
   - Signals =specflow-heading-not-found= if Backlog heading missing
   - Displays "Task added: <HEADLINE>" on success

** DONE ERT: specflow-phase-shift tests
   CLOSED: [2026-01-20]
   5 tests passing.
   - Changes phase successfully
   - Validates phase input (rejects invalid phases)
   - Displays confirmation message
   - Uses existing write infrastructure

** DONE ERT: specflow-add-root-task tests
   CLOSED: [2026-01-20]
   7 tests passing.
   - Adds task to Backlog section
   - Creates TODO (not NEXT)
   - Handles missing Backlog section gracefully
   - Displays confirmation message
   - Minimal-diff write (no other content modified)

* Task Management Commands

** DONE Implement: specflow-refine-task
   CLOSED: [2026-01-20]
   Given a backlog task,
   when =specflow-refine-task= is called interactively,
   then it generates a prompt for Claude and copies to kill-ring.

   Outcomes:
   - Parses Backlog section for level-2 headings
   - Displays task headlines via =completing-read=
   - Prompts for optional unit hint (free text)
   - Prompts for additional context
   - Generates formatted prompt with task, context, guidelines
   - Copies prompt to kill-ring
   - Displays "Task prompt copied to kill-ring"
   - Signals =specflow-heading-not-found= if Backlog missing

** DONE Implement: specflow-activate-task
   CLOSED: [2026-01-20]
   Given a backlog task,
   when =specflow-activate-task= is called interactively,
   then it generates prompt AND promotes task to NEXT.

   Outcomes:
   - All outcomes from =specflow-refine-task=, plus:
   - Finds current NEXT in Active section (if any)
   - Demotes NEXT → TODO (keeps in Active)
   - Moves selected task from Backlog → Active
   - Promotes selected task TODO → NEXT
   - Saves with minimal diff
   - Displays "Task activated: <HEADLINE>"

** DONE ERT: specflow-refine-task tests
   CLOSED: [2026-01-20]
   5 tests, all passing.
   - Lists backlog tasks correctly
   - Handles empty unit input
   - Includes user context in prompt
   - Generates correct prompt format
   - Copies to kill-ring
   - Handles missing Backlog section

** DONE ERT: specflow-activate-task tests
   CLOSED: [2026-01-20]
   6 tests, all passing.
   - Demotes current NEXT to TODO
   - Keeps demoted task in Active section
   - Moves task from Backlog to Active
   - Promotes to NEXT
   - Handles case with no existing NEXT
   - Handles missing sections gracefully

** DONE Simplify: specflow-refine-task and specflow-activate-task
   CLOSED: [2026-01-20]
   REVISED: Original plan to have Emacs move tasks was replaced with prompt-only approach.

   Given a backlog task,
   when either command is called,
   then a prompt is generated and copied to kill-ring (Claude handles file changes).

   Completed:
   - Both commands only generate prompts (no file modifications)
   - Updated prompt ending to "Refine this task and update it in the root todo.org file."
   - specflow-activate-task adds extra instructions for moving to Active as NEXT
   - Both commands display "Task prompt copied to kill-ring"
   - 76 ERT tests passing

* Phase-Shift Enhancement

** DONE Enhance: specflow-phase-shift to also set active unit
   CLOSED: [2026-01-20]
   Given the current phase and active unit from control plane,
   when =specflow-phase-shift= is called interactively,
   then it prompts for phase and unit, and updates both in control plane.

   Outcomes:
   - Read current phase and unit via =specflow-org-store-read-project-state=
   - Prompt for phase with =completing-read= (current as default)
   - Prompt for unit with =completing-read= from registry (current as default)
   - Validate phase is in allowed list
   - Validate unit exists in registry
   - Write both properties to control plane
   - Display "Phase: <PHASE>, Unit: <UNIT>"

** DONE ERT: Update specflow-phase-shift tests
   CLOSED: [2026-01-20]
   8 tests, all passing.
   - Test changing both phase and unit
   - Test changing only phase (keep unit via RET)
   - Test changing only unit (keep phase via RET)
   - Test keeping both (RET, RET)
   - Test invalid phase rejected
   - Test invalid unit rejected (not in registry)

* Validate

** DONE Run full ERT suite
   CLOSED: [2026-01-20]
   79 tests, all passing.

** DONE Manual smoke test
   CLOSED: [2026-01-20]
   - Open a SpecFlow project
   - Call each public function from =M-:=
   - Verify expected output
   All functions tested and returning expected results.

** DONE Smoke test: Task management commands
   CLOSED: [2026-01-20]
   Verified via ERT tests (prompt-only behavior).

   =specflow-refine-task=:
   - PASS: Returns prompt string
   - PASS: Prompt contains task headline
   - PASS: Prompt contains unit hint
   - PASS: Prompt contains user context
   - PASS: Copied to kill-ring
   - PASS: Does NOT modify todo.org

   =specflow-activate-task=:
   - PASS: Returns prompt string with activation instructions
   - PASS: Prompt contains "move to Active as NEXT" instruction
   - PASS: Prompt contains "demote existing NEXT" instruction
   - PASS: Copied to kill-ring
   - PASS: Does NOT modify todo.org
   - PASS: Task body preserved

** DONE Validate: .specflow/ structure migration
   CLOSED: [2026-01-22]
   77 ERT tests passing, manual smoke tests verified.

   =specflow-org-store-find-control-plane=:
   - PASS: Finds =.specflow/specflow.org=

   =specflow-org-store-read-project-state=:
   - PASS: Returns correct phase and active-unit

   =specflow-org-store-read-unit=:
   - PASS: Returns unit without =:rules= key
   - PASS: Spec/Todo paths correct

   =specflow-org-store-validate-unit-pointers=:
   - PASS: Validates only SPEC and TODO (no RULES)

   =specflow-org-store--project-root-from-control-plane=:
   - PASS: Returns correct project root (1 dir up from .specflow/)

* .specflow/ Structure Migration [2026-01-22]

** DONE Update control plane discovery path
   CLOSED: [2026-01-22]
   Given a project with =.specflow/specflow.org=,
   when =specflow-org-store-find-control-plane= is called,
   then it finds =.specflow/specflow.org= (not =docs/specflow.org=).

   Outcomes:
   - [X] Change =specflow-control-plane-filename= default from "docs/specflow.org" to ".specflow/specflow.org"
   - [X] Update =specflow-org-store--project-root-from-control-plane= to go up 1 dir (not 2)

** DONE Remove RULES from unit reading
   CLOSED: [2026-01-22]
   Given a unit entry in the control plane,
   when =specflow-org-store-read-unit= is called,
   then it does NOT require RULES property (only SPEC and TODO required).

   Outcomes:
   - [X] Remove =:rules= from required properties check in =specflow-org-store-read-unit=
   - [X] Remove =:rules= from returned plist (or return nil if present for compatibility)

** DONE Remove RULES from unit validation
   CLOSED: [2026-01-22]
   Given a unit entry plist,
   when =specflow-org-store-validate-unit-pointers= is called,
   then it validates only SPEC and TODO files exist (not RULES).

   Outcomes:
   - [X] Remove RULES validation from =specflow-org-store-validate-unit-pointers=
   - [X] Error message lists only missing SPEC/TODO files

** DONE Update todo.org path for workflow commands
   CLOSED: [2026-01-22]
   Given a project with =.specflow/todo.org=,
   when =specflow-add-root-task=, =specflow-refine-task=, or =specflow-activate-task= is called,
   then they operate on =.specflow/todo.org= (not root =todo.org=).

   Outcomes:
   - [X] Update =specflow-add-root-task= to use =.specflow/todo.org=
   - [X] Update =specflow-refine-task= to use =.specflow/todo.org=
   - [X] Update =specflow-activate-task= to use =.specflow/todo.org=
   - [X] Update prompt text to reference =.specflow/todo.org=

** DONE Update all ERT tests for .specflow/ structure
   CLOSED: [2026-01-22]
   Given the test suite,
   when tests are run,
   then all tests use =.specflow/specflow.org= and =.specflow/todo.org=.

   Outcomes:
   - [X] Update test fixtures: =docs/specflow.org= → =.specflow/specflow.org=
   - [X] Update test fixtures: =todo.org= → =.specflow/todo.org=
   - [X] Remove RULES from test control plane fixtures
   - [X] Remove RULES validation tests (or update to expect no RULES)
   - [X] All tests pass with new structure (77 tests passing)

* Document

** DONE Write units/org-store/README.md
   CLOSED: [2025-01-19]
   Usage examples for each public function.

** DONE Update README.md for .specflow/ structure
   CLOSED: [2026-01-22]
   Given the README.md,
   when a user reads it,
   then examples show =.specflow/specflow.org= paths and no RULES property.

   Changes made:
   - Updated all paths: =docs/specflow.org= → =.specflow/specflow.org=
   - Updated all paths: =root todo.org= → =.specflow/todo.org=
   - Removed RULES from unit plist examples
   - Removed RULES from required properties
   - Updated error table (SPEC/TODO only, no RULES)
   - Updated test count to 77
