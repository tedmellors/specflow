#+TITLE: SpecFlow – Master TODO
#+STARTUP: overview
#+SEQ_TODO: TODO NEXT WAITING | DONE

* About

This is the canonical task driver for the SpecFlow project.
Only ONE task is marked NEXT at a time.

Implementation order: org-store → core → bundle → hydrate → docs → compose → initiate

* Active

** NEXT org-store: Add workflow commands
   Add two interactive commands to org-store:

   1. =specflow-phase-shift= — change current phase in control plane
      - Prompt for phase (Plan, Specify, Scaffold, Implement, Validate, Document)
      - Write to SPEC_FLOW_PHASE property
      - Display confirmation message

   2. =specflow-add-root-task= — append task to root todo.org
      - Prompt for task headline
      - Append as TODO under Active section
      - Optionally prompt for parent unit context

   Dependencies: Uses existing org-store write infrastructure.

* Backlog (future enhancements)
** TODO lock down standard workflow for commiting and pushing
- Plan → Specify → Scaffold - [COMMIT AND PUSH]
- Implement → Validate → Document [COMMIT AND PUSH]
** TODO add rule to auto-save working plan at 10% until compaction as a 'temp-plan-artifact.org'
- this should be added as a reference doc on the NEXT actionable task.
- post compaction we should hydrate.
- we need rules to spend remaining context on saving where we are and documenting rather than trying to sqeeze out more work. 
** TODO need to figure out better system to manage rules rather than dumping everythign into claude.md
** TODO need to have better phase change enforcement
** TODO lock down modular unit structure with src/ and tests/
** TODO Lock down expected todo.org structure/formatting
- requirements for NEXT
- requirements for TODO
- requirements for DONE
** TODO lock down phase workflow always: Plan → Specify → Scaffold → Implement → Validate → Document
** someday:
*** TODO gptel integration for hydrate
*** TODO Transient menus for command discovery
*** TODO LLM compression for bundle

* Completed
** DONE Draft overview.org
   CLOSED: [2025-01-19]
   Purpose, problem statement, principles, success criteria defined.

** DONE Draft architecture.org
   CLOSED: [2025-01-19]
   Components, implementation order, data flow, error handling defined.

** DONE Create control plane
   CLOSED: [2025-01-19]
   =docs/specflow.org= with unit registry and hierarchy.

** DONE Review and approve org-store spec
   CLOSED: [2025-01-19]
   Spec covers discovery algorithm, public API, error handling, minimal-diff writes.

** DONE org-store: Scaffold phase
   CLOSED: [2025-01-19]
   =CLAUDE.md= and =todo.org= written with outcome-based tasks.

** DONE org-store: Implement → Validate → Document
   CLOSED: [2025-01-19]
   All implementation tasks complete. 53 ERT tests passing.
   Manual smoke tests verified. README.md written.

** DONE bundle: Plan → Specify → Scaffold → Implement → Validate → Document
   CLOSED: [2025-01-19]
   Context bundling for AI assistants. 25 ERT tests passing (78 total).
   M-x specflow-bundle command working. README.md written.

** DONE bundle: Enhancement - Dual output modes
   CLOSED: [2025-01-20]
   Added paths mode (default) and text mode for different AI tools.
   - =specflow-bundle= outputs file paths (~20 lines) for Claude Code
   - =specflow-bundle-text= outputs full content (~600 lines) for gptel/web
   90 ERT tests passing (37 bundle + 53 org-store).

** DONE hydrate: Plan → Specify → Scaffold → Implement → Validate → Document
   CLOSED: [2025-01-20]
   AI conversation framing. 25 ERT tests passing (115 total).
   M-x specflow-hydrate-copy-header and 10 other commands working.
   README.md written.

** DONE docs: Plan → Specify → Scaffold → Implement → Validate → Document
   CLOSED: [2025-01-20]
   User-facing documentation. Root README.md written (187 lines).
   All 9 required sections present, validated against spec.

** DONE compose: Plan → Specify → Scaffold → Implement → Validate → Document
   CLOSED: [2026-01-20]
   Task-specific prompt generation. 33 ERT tests passing (148 total).
   5 interactive commands: specflow-compose, new-unit, edit-spec, new-feature, refactor.
   Template buffers with C-c C-c / C-c C-k keybindings. README.md written.

** DONE initiate: Plan → Specify → Scaffold → Implement → Validate → Document
   CLOSED: [2026-01-20]
   Project bootstrapping. 21 ERT tests passing (169 total).
   M-x specflow-initiate creates control plane, CLAUDE.md, todo.org, .specflow marker.
   README.md written.

** DONE compose: Extract to modular structure
   CLOSED: [2026-01-20]
   Extracted compose code to units/compose/src/specflow-compose.el.
   43 ERT tests passing. Documentation updated.

** DONE initiate: Extract to modular structure
   CLOSED: [2026-01-20]
   Extracted initiate code to units/initiate/src/specflow-initiate.el.
   21 ERT tests passing. Documentation updated.

** DONE facade: Convert src/specflow.el to loader
   CLOSED: [2026-01-20]
   Converted monolithic src/specflow.el (1845 lines) to minimal facade (34 lines).
   All 183 tests pass across 5 modules. Modular restructuring complete.


