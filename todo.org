#+TITLE: SpecFlow – Master TODO
#+STARTUP: overview
#+SEQ_TODO: TODO NEXT WAITING | DONE

* About

This is the canonical task driver for the SpecFlow project.
Only ONE task is marked NEXT at a time.

Implementation order: org-store → core → bundle → hydrate → docs

* Completed

** DONE Draft overview.org
   CLOSED: [2025-01-19]
   Purpose, problem statement, principles, success criteria defined.

** DONE Draft architecture.org
   CLOSED: [2025-01-19]
   Components, implementation order, data flow, error handling defined.

** DONE Create control plane
   CLOSED: [2025-01-19]
   =docs/specflow.org= with unit registry and hierarchy.

** DONE Review and approve org-store spec
   CLOSED: [2025-01-19]
   Spec covers discovery algorithm, public API, error handling, minimal-diff writes.

** DONE org-store: Scaffold phase
   CLOSED: [2025-01-19]
   =CLAUDE.md= and =todo.org= written with outcome-based tasks.

** DONE org-store: Implement → Validate → Document
   CLOSED: [2025-01-19]
   All implementation tasks complete. 53 ERT tests passing.
   Manual smoke tests verified. README.md written.

** DONE bundle: Plan → Specify → Scaffold → Implement → Validate → Document
   CLOSED: [2025-01-19]
   Context bundling for AI assistants. 25 ERT tests passing (78 total).
   M-x specflow-bundle command working. README.md written.

* Active

** NEXT bundle: Enhancement - Validate phase
   Given dual output modes are implemented,
   when manual smoke tests pass,
   then the enhancement is complete.

   Enhancement completed:
   - =specflow-bundle= outputs file paths (new default, ~20 lines)
   - =specflow-bundle-text= outputs full content (~600 lines)
   - 90 ERT tests passing

   Current phase: Validate (manual smoke tests pending)
   See =units/bundle/todo.org= for remaining validation tasks.

** TODO hydrate: Plan phase
   Given org-store and bundle are complete,
   when hydrate unit Plan phase is done,
   then we have design clarity for AI workspace commands.

   See =units/hydrate/= for unit artifacts.

* Backlog (dependency order)

** TODO hydrate: Plan → Specify → Scaffold → Implement → Validate → Document
   AI workspace commands (Claude, gptel), buffer/window management.
   Depends on: org-store, core, bundle

** TODO docs: Plan → Specify → Scaffold → Implement → Validate → Document
   User-facing documentation and README. Written last.
